# Development dependencies
tox>=4.0.0
pre-commit>=3.5.0
ruff>=0.1.9
radon>=6.0.1
bandit[toml]>=1.7.6

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Documentation (Note: asciidoctor must be installed via system package manager)
# Install with: sudo apt-get install asciidoctor ruby-asciidoctor-pdf
